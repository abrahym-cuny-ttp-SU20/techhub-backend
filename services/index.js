const userService = require("./userService");
const pageLinkService = require("./pageLinkService");
const skillService = require("./skillService");

module.exports = {
  userService,
  pageLinkService,
  skillService,
};
